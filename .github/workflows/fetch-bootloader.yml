name: Update RPi Firmware Files Hourly

on:
  schedule:
    - cron: '0 * * * *' # Jede Stunde zur vollen Stunde
  workflow_dispatch:     # Manuelles Auslösen per GitHub UI

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Repository klonen
        uses: actions/checkout@v3

      - name: 🧰 curl installieren (falls nicht vorhanden)
        run: sudo apt-get update && sudo apt-get install -y curl

      - name: 🚀 Bash-Skript ausführen
        run: |
          bash build.sh

      - name: 📤 Änderungen committen (falls vorhanden)
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add rpi_firmware/
          if ! git diff --cached --quiet; then
            git commit -m "🔄 Auto-Update RPi firmware files"
            git push
          else
            echo "✅ Keine Änderungen – nichts zu committen."
          fi
